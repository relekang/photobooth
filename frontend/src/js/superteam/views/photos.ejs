
<ul class="photos">
	<% for (var i = 0; i < this.attr("length"); i++) {
		var photo = this[i];
	%>
	<li data-id="<%= photo.id %>" data-image-url="<%= photo.file %>">
		<img src="<%= photo.thumbnail %>"/>
	</li>
	<% } %>
</ul>